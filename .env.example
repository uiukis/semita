NEXT_PUBLIC_SITE_URL=http://localhost:3100

# Maintainer-only: Semita Mini Benchmark / AI Gateway (never expose as NEXT_PUBLIC_*)
# Preferred: `npx vercel env pull .env.local --yes` → VERCEL_OIDC_TOKEN
# Alternative static key: https://vercel.com/ai-gateway → AI_GATEWAY_API_KEY
# Card required on the Gateway page to unlock free credits before real calls.
# AI_GATEWAY_API_KEY=
# VERCEL_OIDC_TOKEN=
